import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    imageCover: {
        flex: 1,
        height: 100,
        width: '100%',
        resizeMode: 'cover'
    },
    imageContain: {
        resizeMode: 'contain'
    }
})

export default styles