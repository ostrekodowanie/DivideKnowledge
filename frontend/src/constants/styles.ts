import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        flex: 1,
        height: 100,
        width: '100%',
        resizeMode: 'cover'
    }
})

export default styles