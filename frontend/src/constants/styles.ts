import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    imageCover: {
        resizeMode: 'cover'
    },
    imageContain: {
        resizeMode: 'contain'
    }
})

export default styles